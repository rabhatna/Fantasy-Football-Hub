# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Local data (draft state, snapshots, caches) — bind-mounted into the container
data/

# Environment
.env
.env.local

# OS
.DS_Store
# macOS/iCloud duplicate copies ("file 2.ts") of generated or build files
* [0-9].ts
.tsbuildinfo [0-9]
